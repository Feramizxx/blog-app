version: '3.8'

services:
  blog-app:
    image: blog-app
    build: .
    container_name: blog-app-container
    ports:
      - "3000:3000"  # Map container port 3000 to host port 3000
    environment:
      - NODE_ENV=production
